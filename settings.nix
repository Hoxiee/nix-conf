# Single configuration point for user-specific values.
# Defaults are set for hoshi — install.sh overwrites this file on new machines.
# Do not add system or hardware configuration here.
{
  username = "hoshi";
  fullName = "hoshi";
  systemStateVersion = "26.05";
}
